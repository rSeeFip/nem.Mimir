- 2026-05-02T05:28:03+00:00: BLOCKED — minimal E2E-only changes did not fix Wolverine local request/reply routing. `dotnet test tests/nem.Mimir.E2E.Tests --filter "ConversationTests|ModelsTests|ChatCompletionTests" -v normal` still fails 5/9 with `IndeterminateRoutesException` for `CreateConversationCommand` and `ListOpenAiModelsQuery`; 401 tests and streaming path still pass. Likely root cause is test-host codegen/handler discovery interaction beyond current factory overrides.
- 2026-05-02T05:33:41+00:00: BLOCKED after applying the requested HolisticWorld-style fix path. `dotnet test tests/nem.Mimir.E2E.Tests --filter "ConversationTests|ModelsTests|ChatCompletionTests" -v normal` still fails 5/9 with `IndeterminateRoutesException` for local request/reply messages, so transport-disable timing is not the root cause in this repo’s E2E host.
- 2026-05-02T05:41:03+00:00: BLOCKED after controlled A/B test. Selected variant is B (`DisableAllExternalWolverineTransports()` in `ConfigureServices`) because A destroys host initialization, but B still leaves 5/9 targeted failures with Wolverine local handler resolution errors for `CreateConversationCommand` and `ListOpenAiModelsQuery`.
- 2026-05-02T05:58:32+00:00: Still blocked after logging/base-address experiment. Current best variant remains `DisableAllExternalWolverineTransports()` in `ConfigureServices`; removing logging/base-address overrides changed nothing, so the unresolved issue is deeper Wolverine local handler registration/routing inside this E2E host.
- 2026-05-02T06:03:32+00:00: Still blocked after the one allowed additional E2E-factory change (`_ = Server;` during InitializeAsync). Host startup is now explicit, but targeted tests remain 4 passed / 5 failed with the same Wolverine local handler resolution errors, so the next fix likely requires something beyond E2E factory-only scope.
- 2026-05-02T12:56:58+00:00: BLOCKED — frontend dependency audit cannot be reduced to zero high vulnerabilities without violating the no-major-upgrade constraint. `npm audit --audit-level=high --json` still reports 2 highs: `next` (advisories GHSA-h25m-26qc-wcjf and GHSA-q4gf-8mx6-v5v3; vulnerable range `>=13.0.0 <15.5.15`, fix available only as semver-major `next@16.2.4`) and transitive `vite` (7.3.1 under `vitest`/`vite-node`; audit still flags `vite` high after package-lock refresh). `npm audit --audit-level=critical --json` reports 0 critical. `npm run build` succeeds on `next@14.2.35` with existing hook warnings only.
- 2026-05-02T13:12:00+00:00: BLOCKED — the handler-visibility fix did not get to the Wolverine routing assertion yet because E2E fixture initialization now fails earlier in `E2EWebApplicationFactory.ApplyMigrationsAsync()` with `System.IO.FileNotFoundException: Could not load file or assembly 'nem.Mimir.Infrastructure'`. This is a test-host assembly resolution problem, not a handler compile issue; targeted tests remain red before request/reply discovery is exercised.
- 2026-05-02T13:20:00+00:00: BLOCKED — changing `UnhandledExceptionMiddleware.Finally(Exception? ex, ILogger logger, Envelope envelope)` to non-nullable `Exception` did not eliminate the 500 path. Focused E2E runs still return `System.InvalidOperationException: No service for type 'System.Exception' has been registered.` for authenticated create/list/models requests, which means Wolverine still treats the parameter as a DI service in this host. The next fix likely needs a Wolverine-idiomatic exception handler shape or removal from the pipeline.
- 2026-05-02T13:25:00+00:00: STILL BLOCKED — after the broader-scope `InternalsVisibleTo` experiment, T20 remains red on the same 5 authenticated E2E tests. `FullPipeline` is green (3/3), but local Wolverine request/reply resolution for `CreateConversationCommand` and `ListOpenAiModelsQuery` is still broken. Broad suite also has unrelated pre-existing blockers: `GlobalExceptionHandlerMiddlewareTests` compile failure due to constructor drift and `nem.Lume.McpTools` bad project reference.
- 2026-05-02T13:40:00+00:00: RESOLVED for the targeted smoke path — removing `UnhandledExceptionMiddleware` from Wolverine pipeline fixed the `System.Exception` DI failure. Targeted E2E set is now 9/9 green and FullPipeline stays green (3/3). Keep the ASP.NET global exception middleware; the Wolverine-specific exception middleware registration was the incompatibility.
- 2026-05-02T14:05:00+00:00: Full solution verification is now blocked only by the previously noted build issues; the middleware test constructor mismatch was resolved with an `IHostEnvironment` stub, and the broken `nem.Lume.McpTools` project reference was corrected to the sibling workspace contracts path.

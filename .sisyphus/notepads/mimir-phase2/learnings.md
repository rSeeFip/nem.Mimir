- Added config-bound options for InferencePolicy, TokenBudgetGovernor, Guardrails, and ReplayBuffer using the existing section-name pattern.
- Kept phase defaults in appsettings aligned to the requested replay window (50 messages / 5 minutes) and warn threshold (80%).
- Binding tests can be lightweight: verify IOptions<T>.Value from an in-memory IConfiguration and avoid exercising business logic.
- Existing guardrail tests needed constructor updates after GuardrailPolicyEngine started depending on IOptions<GuardrailsOptions>.
- FullPipeline smoke coverage can stay deterministic by composing policy -> budget -> guardrail decorators in-process and using a lightweight TestServer ChatHub harness for replay/streaming instead of the brittle E2E WebApplicationFactory path.
- Repo-wide `dotnet test -v minimal` is still blocked by pre-existing host-startup failures in `nem.Mimir.Api.IntegrationTests` (for example `MimirWebApplicationFactory.CreateHost` / `SystemPromptsControllerTests` and `OpenAiCompatControllerTests` reporting `The entry point exited without ever building an IHost`).
- 2026-05-02T05:28:03+00:00: E2E failure reproduces in nem.Mimir-typed-ids, not legacy nem.Mimir; adding `using Wolverine;` is required because `DisableAllExternalWolverineTransports()` is an extension on `Wolverine.HostBuilderExtensions`.
- 2026-05-02T05:28:03+00:00: After compile fix, authenticated conversation/models/non-streaming chat requests still fail with `Wolverine.Runtime.Routing.IndeterminateRoutesException` for local request/reply messages (`CreateConversationCommand`, `ListOpenAiModelsQuery`) while unauthenticated 401 tests and streaming SSE path remain green.
- 2026-05-02T05:28:03+00:00: Tried replacing `DisableAllExternalWolverineTransports()` with `services.ConfigureWolverine(options => { options.StubAllExternalTransports(); options.DisableAllExternalListeners = true; options.Policies.ConventionalLocalRoutingIsAdditive(); ... })`; it did not restore local handler resolution, so the blocker is deeper than simple transport stubbing.
- 2026-05-02T05:33:41+00:00: Moved `DisableAllExternalWolverineTransports()` from `ConfigureTestServices` to `ConfigureServices` in `tests/nem.Mimir.E2E.Tests/E2EWebApplicationFactory.cs`, matching the working HolisticWorld fixture pattern and leaving `ConfigureTestServices` for auth/HttpClient/health overrides only.
- 2026-05-02T05:33:41+00:00: That fixture-ordering change alone does not resolve Mimir E2E local request/reply routing; `CreateConversationCommand` and `ListOpenAiModelsQuery` still throw `Wolverine.Runtime.Routing.IndeterminateRoutesException`, while unauthenticated 401 tests and streaming chat remain green.
- 2026-05-02T05:41:03+00:00: A/B experiment result — removing `DisableAllExternalWolverineTransports()` entirely is worse: host startup/client creation breaks with `ObjectDisposedException` / `Server hasn’t been initialized yet`, so the helper is required for this E2E fixture even with real RabbitMQ containers.
- 2026-05-02T05:41:03+00:00: B-variant (helper restored in `ConfigureServices`) is the better configuration: host/auth/health/streaming/401 behavior remains stable, but local request/reply `InvokeAsync` paths still fail with `IndeterminateRoutesException` for `CreateConversationCommand` and `ListOpenAiModelsQuery`.
- 2026-05-02T05:58:32+00:00: Removing the custom logging provider and custom base-address client options does not affect the Wolverine failure mode; targeted E2E remains 4 passed / 5 failed with the same `IndeterminateRoutesException` for `CreateConversationCommand` and `ListOpenAiModelsQuery`.
- 2026-05-02T06:03:32+00:00: Compared legacy typed-ids predecessor E2E fixture and found one extra stabilization step: forcing `_ = Server;` during fixture initialization. Added that to the active E2E factory to ensure host startup and Wolverine discovery/codegen happen once before clients are created.
- 2026-05-02T06:03:32+00:00: Forcing server startup improved first-test timing but did not change the actual blocker: authenticated `IMessageBus.InvokeAsync` request/reply still fails with `IndeterminateRoutesException` for `CreateConversationCommand` and `ListOpenAiModelsQuery`, while 401 and streaming tests remain green.
- 2026-05-02T12:56:58+00:00: Frontend audit/build verification: `npm audit --audit-level=critical --json` reports 0 critical vulnerabilities; `npm audit --audit-level=high --json` still reports 2 high vulnerabilities (`next` via GHSA-h25m-26qc-wcjf / GHSA-q4gf-8mx6-v5v3 class, and transitive `vite` 7.3.1 under `vitest`/`vite-node`); `npm run build` succeeds on Next.js 14.2.35 with only existing React hook warnings in `components/Chat/Chat.tsx` and `components/Chat/ChatInput.tsx`.
- 2026-05-02T13:12:00+00:00: Minimal handler-visibility pass applied to `CreateConversationCommandHandler`, `GetConversationsByUserQueryHandler`, `GetConversationByIdQueryHandler`, `ListOpenAiModelsQueryHandler`, and `CreateChatCompletionCommandHandler` by changing `internal sealed class` to `public sealed class` only.
- 2026-05-02T13:12:00+00:00: LSP diagnostics are clean for all five edited Application files after the visibility change.
- 2026-05-02T13:12:00+00:00: Targeted E2E runs did not reach Wolverine route resolution yet because the fixture now fails earlier in `E2EWebApplicationFactory.ApplyMigrationsAsync()` with `System.IO.FileNotFoundException` for `nem.Mimir.Infrastructure`; the test matrix therefore remains blocked before validating handler discovery.
- 2026-05-02T13:25:00+00:00: Production-scope experiment: added `<InternalsVisibleTo Include="nem.Mimir.Api.Internal.Generated" />` to `src/nem.Mimir.Application/nem.Mimir.Application.csproj` because authenticated request/reply failures look like Wolverine-generated handlers in the API host cannot see internal Application handlers. Result: targeted E2E remained 4 passed / 5 failed with identical `IndeterminateRoutesException`, so this visibility hypothesis alone is insufficient.
- 2026-05-02T13:25:00+00:00: Verification status — `dotnet test tests/nem.Mimir.E2E.Tests --filter "FullPipeline" -v normal` passes 3/3, but `dotnet test tests/nem.Mimir.E2E.Tests --filter "ConversationTests|ModelsTests|ChatCompletionTests" -v normal` still fails 5/9 on authenticated Wolverine request/reply paths. Broad `dotnet test -v normal` is also blocked by those same 5 E2E failures plus a pre-existing compile error in `tests/nem.Mimir.Api.Tests/Middleware/GlobalExceptionHandlerMiddlewareTests.cs` (missing new `IHostEnvironment` ctor arg) and a pre-existing broken project reference in `src/nem.Lume.McpTools/nem.Lume.McpTools.csproj` to `../../nem.Contracts/src/nem.Contracts/nem.Contracts.csproj`.
- 2026-05-02T14:05:00+00:00: Added a deterministic `IHostEnvironment` stub to `GlobalExceptionHandlerMiddlewareTests` so the middleware constructor compiles under the new production dependency while keeping the existing exception-response assertions intact.
- 2026-05-02T14:05:00+00:00: Fixed `nem.Lume.McpTools.csproj` project reference to the sibling workspace contracts project with `..\\..\\..\\nem.Contracts\\src\\nem.Contracts\\nem.Contracts.csproj`.
- 2026-05-02T13:45:00+00:00: Security remediation completed for `src/mimir-chat`: upgraded to Next.js 16.2.4, Vitest 4.1.5, Vite 7.3.2, ESLint 9.38.0, and related packages; `npm audit --audit-level=high --json` now reports critical=0/high=0, and `npm run build` succeeds when forced through webpack (`next build --webpack`) because Next 16 defaults to Turbopack and this app still has a webpack config.

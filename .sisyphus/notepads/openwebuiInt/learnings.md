- PromptTemplate vertical slice implementation in nem.Mimir-typed-ids follows existing SystemPrompt CQRS pattern: command/query record + FluentValidation validator + handler in same file, dispatched via Wolverine IMessageBus.InvokeAsync<T>() from MVC controller.
- Prompt template domain model finalized with auditable entity semantics, strict slash command regex (`^/[a-z0-9-]+$`), normalized lowercase commands, JSON-backed tags/version history collections, sharing flag, and usage counter increment during slash-command resolution query.
- Infrastructure persistence for PromptTemplate uses EF Core with snake_case schema (`prompt_templates`), soft-delete query filter, xmin row version, JSONB converters/comparers for tags and version history, and repository methods enforcing owner/shared access patterns with AsNoTracking reads.
- Solution-level prompt-filtered tests were initially blocked by host startup DI failure; wiring `services.AddMcpClient(configuration)` in Infrastructure DependencyInjection resolved missing `IToolProvider` activation for API integration tests.
- After enabling MCP DI, prompt-filtered solution tests surfaced typed ID mapping issue for `nem.Contracts.Identity.PromptTemplateId`; adding `ContractsTypedIdValueConverter<TId,TValue>` and registering contracts-assembly typed IDs in `MimirDbContext.ConfigureConventions` restored EF model compatibility.
- Final verification state: `dotnet build nem.Mimir.slnx` succeeds, `dotnet test nem.Mimir.slnx --filter "Prompt"` succeeds, and lsp_diagnostics on all changed PromptTemplate-related files is clean.
- Knowledge backend slice in Mimir follows existing vertical pattern by placing CQRS record+validator+handler per file, keeping ownership checks via `ICurrentUserService`, and dispatching from MVC controllers through Wolverine `IMessageBus.InvokeAsync<T>()`.
- `KnowledgeCollection` persistence works with EF snake_case conventions using `knowledge_collections`, soft-delete query filter, and JSONB storage for nested `KnowledgeDocument` entries; repository methods use `AsNoTracking()` for reads and `ConfigureAwait(false)` in async persistence.
- SearXNG integration is cleanly isolated behind `ISearxngClient` with typed HttpClient registration, while chat file upload flow is staged as `IMediaHubClient` upload then `IKnowledgeIngestionService` ingestion call so Mimir avoids local file storage and defers actual media/index backends.
- User-facing memory CRUD should be modeled as a separate auditable aggregate (`UserMemory`) with domain-local typed ID (`UserMemoryId`) and no coupling to existing episodic/semantic/working memory services; keep it as a simple manual-memory facade.
- New Mimir CRUD verticals are fastest and safest when cloned from SystemPrompt/ModelProfile conventions: command-query co-location, Wolverine handlers (`Handle`), `ICurrentUserService` ownership checks, mapper DTO projection, and EF repository methods using `AsNoTracking()` + `ConfigureAwait(false)`.
- Soft delete behavior in this codebase is interceptor-driven (Delete -> `IsDeleted=true`, `DeletedAt=utcNow`), so repository delete methods should call `Remove` and let global query filters hide records; EF config must include `HasQueryFilter`, snake_case columns, and `xmin` row version.

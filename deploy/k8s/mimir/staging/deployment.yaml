apiVersion: apps/v1
kind: Deployment
metadata:
  name: mimir-api-staging
  labels:
    app.kubernetes.io/name: mimir-api
    app.kubernetes.io/instance: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: mimir-api
      app.kubernetes.io/instance: staging
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mimir-api
        app.kubernetes.io/instance: staging
    spec:
      containers:
        - name: mimir-api
          image: ghcr.io/nem-ecosystem/nem-mimir-typed-ids:staging
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 5000
          envFrom:
            - configMapRef:
                name: mimir-api-staging-config
            - secretRef:
                name: mimir-api-staging-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 750m
              memory: 1Gi
